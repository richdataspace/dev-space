// Babel: Converte um código em outro - Converte um codigo do react para que seja possivel o browser interpretar;
// WebPack: Para cada tipo de arquivo o codigo eh convetido de maneira diferente (.js, .css, .png);

// Loeaders: babel - loader,  css - loader, image - loader, file - loader. (Utilizados pelo webpack);

